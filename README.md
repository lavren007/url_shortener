# Сервис Сокращения Ссылок

Профессиональный сервис для сокращения URL с поддержкой лимитов переходов, TTL и мультипользовательского режима.

## 🚀 Возможности

### Основные функции:

- ✅ Сокращение длинных URL - преобразование длинных ссылок в короткие коды
- ✅ Уникальные ссылки для каждого пользователя - один URL у разных пользователей создает разные короткие ссылки
- ✅ Лимиты переходов - установка максимального количества переходов с автоматической блокировкой
- ✅ TTL (время жизни) ссылок - автоматическое удаление ссылок через 24 часа (настраивается)
- ✅ UUID идентификация пользователей - автоматическая генерация UUID при первом использовании
- ✅ Автоматическое открытие в браузере - переход по оригинальному URL при вводе короткого кода
- ✅ Мультипользовательский режим - изоляция данных между пользователями

## 🛠 Установка и запуск

### Требования:
- Java 11+
- Maven 3.6+

### Установка:
1. Клонирование репозитория:

```bash
git clone <repository-url>
cd url-shortener
```

2.  Сборка проекта

```bash
mvn clean compile
```

3. Запуск приложения

```bash
mvn exec:java -Dexec.mainClass="Main"
```

 Или создание JAR

```bash
mvn package
java -jar target/url-shortener-1.0.0.jar
```
### Запуск в IDE:

- Откройте проект в IntelliJ IDEA, Eclipse или другой Java IDE
- Запустите класс Main.java

# 📖 Использование

### Первый запуск:

- При первом запуске система предложит создать пользователя:

```
👤 Настройка пользователя
1. Создать нового пользователя
2. Использовать существующего (по ID)
```
- Основное меню:

```
🎯 ГЛАВНОЕ МЕНЮ
══════════════════════════════════════════
1.  Создать короткую ссылку
2.  Перейти по короткой ссылке
3.  Удалить ссылку
4.  Мои ссылки
5.  Поиск ссылок
6.  Статистика
7.  Топ популярных
8.  Последние созданные
9.  Изменить лимит переходов
10. Сменить пользователя
11. Все ссылки (админ)
0.  Выход
    ══════════════════════════════════════════
    👤 Текущий пользователь: Ivan (a1b2c3d4-5678-90ef-1234-567890abcdef)
    Примеры использования:
```

1. Создание короткой ссылки:

```
🌐 Введите полный URL: https://www.example.com/very-long-path/page.html
🔢 Установить лимит переходов? (оставьте пустым для отсутствия лимита): 10

✅ Короткая ссылка создана!
🌐 Оригинальный URL: https://www.example.com/very-long-path/page.html
🔗 Короткая ссылка: http://short.url/AbC123
🔑 Код: AbC123
🎯 Лимит переходов: 10
```

2. Переход по короткой ссылке:

```
🔑 Введите короткий код: AbC123

🔗 Короткая ссылка: http://short.url/AbC123
🌐 Оригинальный URL: https://www.example.com/very-long-path/page.html
🖥️  Открыть в браузере? (y/n): y
✅ Открываю оригинальный URL в браузере...
```

3. Просмотр статистики:

```
=== Статистика ===
Всего ссылок: 15
Всего пользователей: 3
Активных ссылок: 12
Всего переходов: 247
Средних переходов на ссылку: 16.47

Самая популярная ссылка:
XyZ789 -> https://popular-site.com [переходов: 89 (без лимита), создана: 2024-01-15T10:30:45, истекает: 2024-01-16T10:30:45] - АКТИВНА
```

# ⚙️ Конфигурация

- Все настройки находятся в файле config.properties:

```
# Базовый URL для коротких ссылок
base.url=http://short.url/

# Длина короткого кода (рекомендуется 4-8 символов)
short.code.length=6

# Время жизни ссылок в часах (по умолчанию 24 часа)
default.url.ttl.hours=24

# Максимальная длина оригинального URL
max.url.length=2048

# Интервал очистки просроченных ссылок в минутах
cleanup.interval.minutes=30
```

# 🏗 Архитектура проекта

```
url-shortener/
├── src/
│   ├── main/java/
│   │   ├── Main.java                    # Точка входа, CLI интерфейс
│   │   ├── UrlShortenerService.java     # Основная бизнес-логика
│   │   ├── ShortUrl.java                # Модель сокращенной ссылки
│   │   ├── User.java                    # Модель пользователя
│   │   ├── Config.java                  # Загрузка конфигурации
│   │   └── exceptions/
│   │       ├── InvalidUrlException.java
│   │       ├── UrlNotFoundException.java
│   │       └── UrlExpiredException.java
│   ├── test/java/                       # Юнит и интеграционные тесты
│   └── resources/config.properties      # Конфигурация приложения
├── pom.xml                              # Maven конфигурация
└── README.md                            # Документация
```
# 🧪 Тестирование

### Запуск тестов:

```bash
  # Все тесты
mvn test
```
```bash
# Конкретный тестовый класс
mvn test -Dtest=UrlShortenerServiceTest
```
```bash
   # Конкретный тестовый метод
mvn test -Dtest=UrlShortenerServiceTest#testCreateShortUrl_ValidUrl
```
```bash
# С отчетом о покрытии
mvn test jacoco:report
```
## Структура тестов:

### Unit тесты - тестирование отдельных компонентов

- UrlShortenerServiceTest - основная бизнес-логика
- ShortUrlTest - модель данных ссылки
- UserTest - модель пользователя

### Интеграционные тесты - комплексные сценарии использования

- IntegrationTest - полные пользовательские сценарии
